services:
  - type: web
    name: universal-copilot
    runtime: docker
    plan: free
    envVars:
      - key: PORT
        value: 10000
      - key: ENCRYPTION_SECRET
        generateValue: true
      - key: DATA_DIR
        value: .
